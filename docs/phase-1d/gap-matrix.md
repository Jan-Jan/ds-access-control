# Phase 1.d gap matrix

| Library | Gate | Flow | Principal | Severity | Subcrate | Fix path | Fix effort | Phase 3 effort | Notes |
|---|---|---|---|---|---|---|---|---|---|
| Panda | 0 | A | NA | Hard | p2panda-sync | Fork | Large | Medium | core: PASS (getrandom/js pin); auth: PASS; encryption: PASS (getrandom 0.3/wasm_js pin); spaces: PASS (same pin); net --no-default-features: PASS (tokio_with_wasm); p2panda-sync: FAIL — mio 1.2.0 (std-only I/O poller) via tokio default-features=true |
| Panda | 1 | A | Member | Hard | p2panda-spaces | TraitImpl | Small | Small | L1: p2panda-auth None, p2panda-spaces Hard. L2: both materialise_actor_id and ResolverPki<IdentityRegistry> implemented and tested. Rotation-tracking confirmed at runtime (Probe 3 from Task 4). ActorId::from(PandaVK) construction path: dalek::VK → p2panda_core::VK → ActorId. 7/7 tests pass. IdentityRegistry static-method escape hatch: Y=ResolverPki<R>. L3: revocation+org_pseudo_group scenarios exercise gate-1 ACL (l3_revocation.rs, l3_org_pseudo_group.rs). |
| Panda | 2 | D | NA | Hard | p2panda-spaces | Fork | Small | Small | L1: p2panda-auth None (Groups trait pub, externally implementable, 5/5 tests pass). p2panda-spaces Hard (AuthGroupState<C> in private mod types; compile error E0603 confirmed). L2: best-effort — trie-only-write invariant confirmed at CRDT layer (3/3 tests pass). Spaces runtime deferred: architecturally blocked by private type, not time-boxed. Forge trait secondary intercept is syntactically possible but weaker. Inventory hypothesis was Soft/TraitImpl; actual is Hard/Fork for spaces layer. |
| Panda | 3 | B | Member | Soft |  | TraitImpl | Small | Small | L1: IdentityRegistry::identity_key via ResolverPki confirmed; full Dcgka::create + Dcgka::process round-trip with KeyRegistry<DcgkaMemberId> as PKI confirmed. Orphan-rule friction: DcgkaMemberId newtype required. DcgkaError requires ID: Display. test_utils feature needed for KeyManager::init_and_generate_prekey in test setup. L3: revocation (Dcgka::remove + forward-security) and org_pseudo_group (trigger_recompute on rotation) scenarios (l3_revocation.rs, l3_org_pseudo_group.rs). Integration finding: revocation uses Dcgka::remove (not trigger_recompute) — Dcgka::update sends DMs to all DGM members including removed ones. |
| Panda | 3 | C | Member | Soft |  | TraitImpl | Small | Small | L1: identity_key via ResolverPki reflects rotation immediately (resolver-level proof). Full Dcgka::update + process round-trip after PKI rebuild confirmed. Epoch secrets differ; shared secret verified. L2: combined Flow B+C integrated test passes. Both resolver-layer and DCGKA-layer evidence collected. L3: org_pseudo_group rotation drives trigger_recompute; epoch-1 secret shared by alice+bob confirmed (l3_org_pseudo_group.rs). |
| Panda | 4 | A | Org | Hard | p2panda-spaces | Fork | Small | Small | L1 auth: GroupMember::Group accepted by GroupCrdt::process; members() auto-resolves nested groups (test4+test5 pass). Manage-access group rejected (test6). L2 Flow A: both alice and bob appear in effective_member_keys via ORG pseudo-group. Flow C: rotation tracked via resolver — CRDT unchanged, effective key set updated. 6 L1 + 2 L2 tests pass. L3: org_pseudo_group exercises OrgPseudoGroupAdapter + effective_member_keys + DCGKA epoch (l3_org_pseudo_group.rs). |
| Panda | 4 | C | Org | Hard | p2panda-spaces | Fork | Small | Small | L2 Flow C: alice key K1 present before rotation; trie.stub_rotate_member_key updates to K2; effective_member_keys returns K2 (not K1); CRDT still lists alice by stable ID. Hard severity cascades from spaces-layer ActorId hardwiring (same root cause as Flow A). Auth-layer severity for Flow C: None. L3: org_pseudo_group confirms CRDT unchanged + alice K2 in effective set + epoch advance (l3_org_pseudo_group.rs). |
| Panda | 5 | E1 | Member | Soft |  | TraitImpl | Small | Small | L1: policy_check accepts authorised member, rejects unauthorised (PolicyError::Unauthorised) and unknown peer (PolicyError::UnknownPeer). 4 tests pass. SessionConfig::remote field confirmed in p2panda-sync/src/lib.rs (field name is remote, not remote_peer — spec typo corrected at implementation time). L3: gating scenario exercises policy_check accept/reject + DocAcl::for_members (l3_gating.rs). |
| Panda | 5 | E2 | Org | Soft |  | TraitImpl | Small | Small | L1: e2_connection_accepted_via_org_principal confirms alice+bob accepted, charlie rejected. e2_connection_rejected_member_removed_from_org confirms org-removed peer rejected via is_member check. 2 tests pass. |
| Panda | 5 | F1 | Member | Soft |  | TraitImpl | Small | Small | L2: f1_terminate_session_on_member_revocation — alice revoked, session 1 flagged, bob session unchanged. f1_recheck_idempotent_on_already_flagged_sessions — second recheck returns 0. 2 tests pass. L3: gating scenario confirms bob's session flagged + fresh connection rejected; alice's session unchanged; full fixture round-trip (l3_gating.rs). |
| Panda | 5 | F2 | Org | Soft |  | TraitImpl | Small | Small | L2: f2_terminate_org_session_on_member_removal — alice removed from org, session 10 flagged, bob session 20 unchanged. f2_all_sessions_terminated_when_org_emptied — both sessions flagged. 2 tests pass. |
| Keyhive | 0 | A | NA | None |  | None |  | Small | keyhive_crypto: PASS (getrandom 0.2/js pin); beekem: PASS with std (default-features=false mode is broken at the pin — upstream bug, not WASM-related); keyhive_core: PASS — INVENTORY HYPOTHESIS REJECTED (predicted DEFINITE FAIL on tokio+futures, but actual compile succeeds because only tokio::sync slices are used). spike-keyhive itself rejects wasm32 only because it requests tokio rt-multi-thread for native tests — spike-tooling concern. |
| Keyhive | 1 | A | Member | Hard | keyhive_core | TraitImpl | Small | Small | DESIGN PIVOT (2026-06-03): the lazy-CGKA / two-tier model commits to trie-publishes-VerifyingKey-only and defers CGKA placement to member come-online. phase3_effort is Small (original API-only prediction restored after the 2026-06-02 interim revision). The integration finding (Individual::new requires KeyOp) is a footnote about the high-level convenience API. L2 (l2_g1) exercises the eager-add path; L3 (l3_lazy_onboarding) validates the lazy semantics end-to-end on first run, no integration finding. |
| Keyhive | 2 | D | NA | Soft | keyhive_core | TraitImpl | Small | Small | MembershipListener on_delegation/on_revocation return F::Future<'a, ()> — purely post-fact, cannot refuse. CiphertextStore is swappable but governs only encrypted content, not membership. DelegationStore/RevocationStore are concrete fields, not generic. Containment-via-wrapper is the viable substitution. |
| Keyhive | 3 | B | Member | Soft |  | TraitImpl | Small | Small | Cgka::add takes the public key as a direct argument — strictly better than p2panda's IdentityRegistry trait-thread pattern. BeeKEM is O(log n) per update vs DCGKA O(n). No generic-trait threading required. |
| Keyhive | 3 | C | Member | Soft |  | TraitImpl | Small | Small | Cgka::remove(id, signer) and Cgka::update(new_pk, new_sk, signer, csprng) are distinct entry points — no integration finding like p2panda's Dcgka::remove-vs-update issue. Forward-security via fresh PcsKey on every rotation. |
| Keyhive | 4 | A | Org | Soft |  | TraitImpl | Small | Small | STRONG POSITIVE vs p2panda — Keyhive's Agent enum has a first-class Group variant; add_member accepts ANY Agent variant with identical signature. p2panda required a fork (pub use in p2panda-spaces) to access GroupMember::Group at the auth layer. |
| Keyhive | 4 | C | Org | Soft |  | TraitImpl | Small | Medium | Reverse-index 'org -> docs' is maintained by spike code (not Keyhive). Phase 3 effort Medium reflects the index-maintenance code paths. Same reverse-lookup gap as gate 5 (foundation-layer concern). |
| Keyhive | 5 | E1 | Member | Soft |  | TraitImpl | Small | Small | Reverse-lookup gap shared with p2panda — adapter.member_id_for cold lookups need a formal MemberKeyResolver::find_member_by_device method. Push-based termination via on_revocation (better than p2panda pull-based). |
| Keyhive | 5 | E2 | Org | Soft |  | TraitImpl | Small | Small | Builds on gate-4 wrapper. No transport-layer post-open window (Keyhive has no transport — the spike session model is in-process). |
| Keyhive | 5 | F1 | Member | Soft |  | TraitImpl | Small | Small | Better than p2panda — push-based via on_revocation listener vs p2panda pull-based recheck. Open sessions flagged immediately after revocation propagates. |
| Keyhive | 5 | F2 | Org | Soft |  | TraitImpl | Small | Small | Cascade flow: on org member change, flag all sessions for all docs where the org has access. Reuses the gate-4 reverse-index. |

## Per-library summary

- **Keyhive** — hard: 1, soft: 9, total burden: 22
- **Panda** — hard: 5, soft: 6, total burden: 32
